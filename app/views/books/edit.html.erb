<div>
  <p>title : <%= @book.title %></p>
  <p>body : <%= @book.body %></p>
  
  
  
  
</div>

<%= form_with model: @book,  local:true do |f| %>
 <h4>title</h4>
 <%= f.text_field :title %>
 <h4>body</h4>
 <%= f.text_area :body %>
 <%= f.submit '投稿' %>
 <% end %>
 
 <%= link_to 'Show', book_path(@book.id) %> |
 <%= link_to "Back", "/books" %>